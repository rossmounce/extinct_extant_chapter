library(phangorn)
setwd("/home/ross/workspace/rtreedist/newtest")
treeset1 <- read.tree("jack1.tre")

unlist(lapply(1:3, function(x) x^2))

where x are reference trees returns vector of SD of all jacktrees against reftree 
unlist(lapply(1:3, function(x) treedist(x,jack[[1]])))
